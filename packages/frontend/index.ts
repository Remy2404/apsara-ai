import { registerRootComponent } from 'expo';
import 'expo-router/entry';

// This registers the Expo Router entry point
// registerRootComponent is handled internally by expo-router/entry
// No need to explicitly register a component
